# 数据覆盖与缺口勘察报告

勘察日期：2026-09-10。来源：VPS `110.42.34.12` 生产库 `gupiaodb`（Docker 容器 `gupiao-postgres-1`，`apache/age:release_PG15_1.6.0`，352 MB）。访问方式：SSH 隧道 `-L 15432:127.0.0.1:5432`（本地物理网络对 5432/15432 出站有策略拦截，22 端口可用）。

## 1. 结论摘要

**生产库不具备支撑 M7 策略门槛判定的数据基础。** 核心问题不是数据量小，而是**关键链路的产物全部为空**：从未有过一次成功的每日推荐，因此没有推荐、没有证据、没有对账、没有收益。

据此，M7 的"≥60 个独立测试交易日、≥200 笔有效成交"上线门槛**无法在真实历史数据上评估**，只能走计划允许的仿真验收路径，并输出"数据不足、保留 `baseline-v1`"的结论。这不阻塞代码开发。

## 2. 表行数清单（实测）

| 表 | 行数 | 说明 |
|---|---|---|
| `Candle` | 855,961 | 唯一有实质数据的事实表 |
| `Stock` | 5,216 | 行业字段 100% 为空 |
| `StockExposureFact` | 17,726 | 4,658 只股票，**仅 2026-09-07 08:00 的单次快照** |
| `RawNewsRecord` / `NormalizedNewsRecord` | 4,439 / 4,444 | 90% 集中在 9/7–9/8 |
| `CausalSignalCandidate` | 70 | 全部 `asOf = 2026-09-07 08:00` |
| `RunTrace` | 13 | **0 SUCCESS**：11 PENDING、2 FAILED（另有更早的 FAILED 记录） |
| `PipelineStepTrace` | 79 | 仅早期步骤 SUCCESS |
| `AiWorkflow` | 1 | `causal-workflow-v2`，状态 `PAUSED` |
| `EvidenceContribution` | **0** | 推荐链路从未走到评分 |
| `GraphSnapshot` | **0** | |
| `MarketSignalSnapshot` | **0** | |
| `RecommendationSnapshot` | **0** | **无任何推荐历史，无对账，无收益** |
| `KeywordPerformancePenalty` | **0** | |
| `StrategyRun` / `StrategyRecommendationEvent` | **0** / **0** | 无策略实验历史 |
| `ExpectationGapSnapshot` / `ThemeForecast` | **0** / **0** | |
| `StockFeatureSnapshot` / `FactSnapshot` / `CoverageGapCase` / `HistoricalLimitUpCase` | 0 | |

## 3. 行情数据（`Candle`）

- 覆盖区间：**2026-01-05 → 2026-09-07**，共 **165 个交易日**，5,216 只股票。
- 字段仅 `open/high/low/close/volume/capturedAt`，**无**交易状态、涨跌停价、复权口径、公司行动、可见时间。
- 索引：唯一键 `(stockId, tradingDay)`，另有 `tradingDay` 单列索引。
- 价格口径：最低 0.77 元、最高 2,656.90 元；最后交易日 4,378 / 5,216 只 ≤ 40 元。2026-08 起 130,238 个相邻交易日对中有 **964 对涨跌幅超过 10.5%**，最大 27.44% —— 表明存在**未复权的除权跳空**，且无涨跌停字段可判别。
- 股票关联：`Candle.stockId` → `Stock.id`（形如 `stock-600519`），非 symbol。

## 4. 交易日历缺口（实证）

`backend/src/types/value-objects/trading-calendar.ts` 只硬编码 **2024–2025** 假期，**不覆盖 2026**。用 Candle 反推得 2026-01-05~09-07 的真实非交易日：

```
2026-02-16 ~ 02-20, 02-23   （春节）
2026-04-06                  （清明）
2026-05-01, 05-04, 05-05    （劳动节）
2026-06-19                  （端午）
```

即 176 个工作日中有 **11 天实际非交易日**。现状代码会把春节、劳动节当成交易日处理 —— 实证了 M5「工作日即交易日」反模式的真实危害。

**可用资产**：Candle 的 165 个真实交易日足以**反推并生成版本化交易日历种子数据**。

## 5. 新闻覆盖（回测可用性）

- 总量 4,444 条，但 **2,460 条在 2026-09-08、1,568 条在 2026-09-07**。
- 2026 年以来有新闻的日子 119 天，与交易日重叠 **96 天**。其余交易日新闻量仅 1–18 条。
- 最近 10 个交易日新闻量：9/7 为 1,568；9/4 为 17；9/3 为 18；9/2 为 7；9/1 为 5；8/31 为 8；其余 ≤ 13。

**结论**：真正具备"可用新闻量"的交易日只有 **1 天**（9/7）。跨日期增量与历史回放无法基于真实新闻做有意义的多日验证，必须使用固定新闻快照合成数据集。

## 6. 其他缺口

| 缺口 | 影响 |
|---|---|
| `Stock.industry` **100% 为空** | M6 行业动量惩罚、经营变量分类、`backtest-engine` 按 asOf 取行业全部无效 |
| 无 `isST` / 上市退市 / 行业历史 | 无法按历史状态回放，会把今天的名单套到过去 |
| 无交易状态 / 涨跌停价 | M5「停牌或开盘触及涨停不成交」的成交可行性判定无输入 |
| 无收益元数据（status/成熟时间/计算版本） | 分周期收益对账无结构承载 |
| 无暴露历史（仅单日快照） | 暴露刷新阶段无法历史回放 |
| 无独立发布记录 | 已被 `isPublished` 布尔位替代，M4 需补 |

## 7. 失败原因考古（对设计的启示）

`RunTrace` 失败原因是**真实生产故障样本**，直接印证了 M1/M3 的必要性：

| 失败原因 | 对应缺口 |
|---|---|
| `All AI candidates failed`（12 个模型全失败：超时 / 429 / 403 / 504 / 522 / 404） | 供应商全线不可用时应进入 `NEEDS_ATTENTION` 而非整体失败；批次规划与模型选路需要更细的故障隔离 |
| `Prisma transaction expired (5000ms) on stockExposureFact.upsert()` | 大阶段必须分片提交；网络调用不得放在事务内 |
| `Missing TICKFLOW_API_KEY` / `Missing or invalid --aktools-board-limit` | 运行配置校验应在阶段开始前完成 |
| 大量 trace 停在 `PENDING` 且无 `completedAt` | 断点未覆盖 AI 之后的下游阶段，无法恢复 |
| `LLM returned SSE error events` | 流式错误必须整次失败，不降级（与铁律一致） |

## 8. 已就绪的测试基建

- 独立测试库 **`gupiao_test`** 已在 VPS 创建（owner `gupiao`，`age` 扩展已装），与生产库 `gupiaodb` **物理隔离**。
- 全部 **14 个 Prisma 迁移**已成功应用到 `gupiao_test`。
- 以 `AI_TEST_DATABASE_URL` 指向 `gupiao_test` 后，此前被跳过的 **18 个集成用例全部真实执行并通过**。
  - 注意：其中 2 个用例耗时 5.5s / 5.1s，**超过 vitest 默认 5s 超时**，需在 `vitest.config.ts` 提升集成测试超时（当前用 `--testTimeout=30000` 验证通过）。此改动归属 C3。
- 本地访问方式（网络策略所限必须走隧道）：
  ```bash
  ssh -f -N -L 15432:127.0.0.1:5432 root@110.42.34.12
  export AI_TEST_DATABASE_URL='postgresql://gupiao:***@127.0.0.1:15432/gupiao_test'
  ```

## 9. 对计划的修正建议

1. **M5 的行情契约扩展需要新数据源。** 现有 AKTools / Yahoo 源缺交易状态与涨跌停价，需评估可补范围；补不到的字段按计划标为"未知"，不得推测填充。
2. **M7 门槛判定改为主路径 = 仿真验收**：固定新闻快照 + 固定模拟供应商 + 固定随机种子，产出基准表；真实数据结论记录为"数据不足以判定，保留 `baseline-v1`"。
3. **交易日历种子数据可由 Candle 反推生成**（165 个真实交易日），这是把 M5 从"无数据可依"变成"可落地"的关键捷径。
4. **行业数据需要新增采集任务**，否则 M6 的行业动量与经营变量维度不可用；在此之前应在 `DATA_COVERAGE_REPORT` 中显式标注为数据缺口，而不是静默产生空值结果。
