# 计划开发完成度审计

审计日期：2026-09-08。对象：当前本地工作区（含未提交改动）。

结论：**未全部完成开发，不能按完整计划验收。** 上一轮“执行完成”只能指部分本地实现，不能代表可靠续跑、优化效果和部署验收全部完成。

已具备：统一 AI 客户端、自适应并发和限流框架、持久化 AI 队列/租约/重试预算、批次动态调整、内容指纹缓存、逐新闻无信号结果、部分下游阶段复用。以下列的是剩余缺口，不要求重写已完成的全部代码。

## 核心待开发项

### D1 · P0 · 修复 AI 完成时的恢复状态缺口

- 证据：`backend/src/services/ai-causal-workflow.ts:68` 领取任务不接受 `AI_COMPLETE`；同文件第 323 行先写该状态；`backend/scripts/run-daily-recommendation.ts:1199` 仅在阶段 SUCCESS 且有候选时跳过 AI。
- 触发：AI 任务全部提交后、`completeStepTrace` 前崩溃。租约过期后续跑仍被状态检查拒绝。
- 实现：根据持久化任务结果恢复 AI 阶段汇总，明确 AI_COMPLETE/终止失败/可重试失败的转换；阶段成功标记缺失不能导致重发已成功 AI 请求。
- 验收：在 AI_COMPLETE 写入后强制退出，重启同 trace，无新增 AI 请求并能推进下游；全无信号结果仍停止推荐，且不反复调用 AI。

### D2 · P0 · 将排他锁和检查点覆盖完整日推荐流程

- 证据：`backend/scripts/run-daily-recommendation.ts:1192` 准备完成才建 AI session；第 1227 行关闭 session，清除租约与 deadline timer。已成功 AI 的恢复分支不领取该锁。
- 实现：增加运行级租约及完整运行期限；新闻、暴露准备阶段也保存可恢复产物。AI 任务锁可独立释放，运行级锁需覆盖下游。补齐相关入口的恢复策略。
- 验收：两个进程同时恢复同一 trace，仅一个能够写下游；准备阶段失败后复用已落盘输入；下游超出运行期限时安全暂停并可恢复。

### D3 · P0 · 建立阶段依赖与缓存失效规则

- 证据：`backend/src/services/trace-manager.ts:28` 可覆写既有 trace 的 asOf/cluster/kind；`backend/src/services/backtest-engine.ts:112` 评分复用只比较行数，第 161 行推荐复用独立判断，未因上游重算而失效；第 235 行复用对账汇总。
- 实现：校验 trace 不可变输入；持久化各阶段输入、算法/配置版本和产物指纹；上游变化使依赖阶段失效。产物与完成状态原子提交，或使用可验证的幂等提交协议。策略执行独立记录成功状态。
- 验收：相同输入续跑不重算；更改 asOf 被拒绝；评分配置变化不能命中旧结果；删除一条产物后重建的结果与干净运行一致；任意提交边界崩溃不混用新旧评分、推荐或策略结果。

### D4 · P1 · 完成内容缓存的版本管理

- 证据：`backend/src/services/ai-causal-workflow.ts:12` 固定 durable Prompt 版本，第 120 行按允许模型版本复用，第 213 行 manifest 使用固定 Prompt 标签；成功 AI 阶段被脚本跳过时不执行 manifest 校验。
- 实现：统一普通与 durable 抽取的缓存契约，纳入实际 Prompt、Schema、模型及影响输出的请求参数版本；跨 trace 缓存使用确定的完整抽取结果，避免混合多次历史结果。调度并发、超时或供应商排序变化不能无谓失效。明确历史空指纹行继续不复用或安全回填的策略。
- 验收：相同内容换 ID 命中，正文或语义版本变化不命中；仅调并发/超时仍命中；无信号结果正确复用；遗漏、未知 ID、重复无信号 ID、信号与无信号重叠均拒绝，且无部分写入。

### D5 · P1 · 完成按模型能力调整批次及优化验证

- 证据：`backend/src/services/ai-batch-planner.ts:24` 附近对全部模型取最小上下文/输出额度；`backend/src/services/ai-causal-workflow.ts:242` 使用 trace 内混合历史样本；`backend/src/services/ai-transport.ts:65` 用 UTF-8 字节数记录 token 性能数据。
- 实现：按实际可用模型能力组批并在切换模型时重新校验；性能样本按模型归属统计，优先采用实际 usage，保留有标记的保守估算；明确吞吐、失败率、延迟及成本的优化目标。
- 验收：128000 上下文模型可利用自身容量；小上下文模型不会接到超限批次；固定新闻集比较现有方案与新方案的成功新闻/分钟、请求次数、截断率、耗时和成本。没有基准证据，不能宣称“最优”。原来的固定 3–5 条已被后续批次重写要求取代，不作为验收标准。

### D6 · P1 · 补齐模型故障隔离和超时观测

- 证据：`backend/src/services/ai-adaptive-scheduler.ts:112` 在 modelOnly 过滤前更新供应商健康率；`backend/src/services/ai-transport.ts:31` HTTP headers 到达即清除首响应计时，第 23 行将超时统一归为 network。
- 实现：模型结构/长度等故障只影响对应模型；区分账户/供应商故障与模型故障。明确首响应计时口径，分别记录首响应、无进展和总超时，支持可诊断的错误类型。
- 验收：单模型持续结构错误不降低同供应商其他模型健康；共享账户 429 仍共享限流；headers 立即返回但内容延迟、流停滞、总时长耗尽分别按约定超时并留下正确诊断。

## 其他高收益改造中的已确认缺口

以下与可靠推荐及历史重放直接相关；不能据此声称此前已确定所有评分/回测重写细节。

### D7 · P0 · 隔离未完成批次与正式推荐

- 证据：`backend/scripts/run-daily-recommendation.ts:113` 的 publish 函数仅查成功快照并返回 PUBLISHED，没有持久化发布切换；`backend/src/services/temp-stock-recommendation-service.ts:1021` 次日排除查询未限制成功/发布批次。
- 实现：定义正式生效批次及原子发布机制；次日股票/关键词排除等业务读取只能使用正式生效结果；逐个核对页面和历史查询的读取口径。
- 验收：推荐写入后下游失败，其候选不会影响次日排除或正式展示；新批次失败时仍保持原成功批次；重复发布幂等。

### D8 · P0 · 修复历史关键词惩罚的收益可见时间

- 证据：`backend/src/services/keyword-performance-penalty-service.ts:71` 只限制推荐 asOf 和 isReconciled，读取全部 yield1/3/5；没有对各收益实现时间施加当前回放 asOf 限制。
- 实现：为不同收益周期定义成熟/可见时间，惩罚只使用当时已可见收益。区分推荐时点与收益评估时点，补齐分周期对账状态。
- 验收：数据库已有 D+5 收益时，重放 D+1 不能利用 D+5 亏损；尚未成熟收益不触发惩罚。收益评估读取后续行情本身合理，限制的是它向历史评分和惩罚回流。

## 待验收与交付

- 本轮重新运行 `cd backend && bun run check:fix`：typecheck 通过；387 tests 通过、2 失败、11 跳过。失败为 `backend-http-runtime.test.ts:162` 与 `backend-http-server.test.ts:60` 的 Windows/POSIX 路径预期差异。需修复或明确平台适用测试后才能取得全绿结果。
- PostgreSQL 集成测试已有代码，但 `AI_TEST_DATABASE_URL` 未配置，11 项未执行；不能把它们记作通过。补足上述恢复缺口测试后，在真实测试数据库执行，验证租约竞争、断线、事务回滚及崩溃恢复。
- `check:fix` 实际仅 typecheck + test，package.json 无 lint 脚本；尚不能据此证明满足“后端交接无 lint error”，需建立并执行检查。
- 缺少当前改动下 `2026-09-07 16:00` 历史边界真实全流程验收，以及中断后恢复的结果一致性与请求节省证据。
- 新迁移文件存在不等于已应用。尚无当前版本完成提交、推送、VPS 同提交迁移和部署验证的证据；上线应核验准确提交与迁移状态。

建议开发顺序：D1 → D2 → D3 → D7/D8 → D4 → D5/D6 → 数据库及端到端验收 → 部署验证。每项均应以对应验收条件通过为完成标准。
